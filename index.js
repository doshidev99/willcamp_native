import { registerRootComponent } from "expo";

import React from "react";
import { AppRegistry, View, Text } from "react-native";

import App from "./App";


registerRootComponent(App);
// AppRegistry.registerComponent("main", () => AddComponent);
